.settings {
  @mixin modal;
  @mixin wrapper;
  background: rgba(0, 0, 0, 0.34);
  backdrop-filter: blur(1px);

  form {
    width: 70%;
    display: flex;
    flex-direction: column;
    gap: 15px;
  }
}
